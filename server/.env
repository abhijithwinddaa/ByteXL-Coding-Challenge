# If provided, uses OpenWeather (more descriptive conditions)
WEATHER_API_KEY=your_openweather_api_key

# Optional: city name for OpenWeather (fallback to Hyderabad if omitted)
WEATHER_CITY=Hyderabad

# Optional: server port
PORT=3001

# Optional: use Quotable for quotes instead of mock
USE_EXTERNAL_QUOTES=true